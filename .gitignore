# Original full-resolution photos (backed up locally, too large for git)
originals_backup/

# Compression script (not needed on the site)
compress_images.py

# Local tooling and build artifacts (not part of the site)
fun photos-fo prints/.claude/
fun photos-fo prints/tiktok_build/
